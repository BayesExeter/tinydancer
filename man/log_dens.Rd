% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slice_sampler_core.R
\name{log_dens}
\alias{log_dens}
\title{Calculate Log Density}
\usage{
log_dens(x, this_levels, final_levels, implausibility)
}
\arguments{
\item{x}{A numeric vector representing the point for which the log density is to be calculated.}

\item{this_levels}{A numeric vector specifying the thresholds for implausibility at the current level. \code{x} will be valid if implausibility is below \code{this_levels}}

\item{final_levels}{A numeric vector specifying the target thresholds for implausibility at the final level. Required to pass to implausibility.}

\item{implausibility}{A user-defined function that calculates the implausibility of a point \code{x} given the \code{final_levels}.
It must accept \code{x} and \code{final_levels} as arguments and return a numeric vector.}
}
\value{
Returns \code{1} if all calculated implausibilities are less than or equal to \code{this_levels},
and \code{-Inf} otherwise.
}
\description{
This function calculates the log density of a point \code{x} based on the given
implausibility function, levels, and box limits.
}
\examples{
# Define a simple implausibility function
implausibility <- function(x, final_levels) { abs(x) - final_levels }

# Define parameters
x <- c(0.2, 0.3)
this_levels <- c(0.3, 0.7)
final_levels <- c(0.3, 0.7)

# Calculate log density
result <- log_dens(x, this_levels, final_levels, implausibility)
print(result)
}
